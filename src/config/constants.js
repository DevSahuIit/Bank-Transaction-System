// Save this as src/config/constants.js

module.exports = {
    MAX_FUNDS_REQUEST_AMOUNT: 5000,   // cap per single request
    MAX_TOTAL_FUNDS_PER_USER: 20000   // lifetime cap per person, checked across all their accounts
}